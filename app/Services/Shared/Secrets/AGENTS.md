# AGENTS.md

This directory is covered by the nearest parent `AGENTS.md` in the repository tree.
Follow the closest ancestor instruction file before making edits in this directory.

- Keep secret persistence scoped and decoupled from provider-specific logic.
- Never store provider credentials in plaintext.
- Handle storage/decryption errors gracefully and fail closed.

